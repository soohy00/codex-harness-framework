# This framework does not require runtime secrets by default.
# Add only the variables your own project needs after copying the framework.
#
# Example:
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# CUSTOM_API_BASE_URL=
